# День 10: Подключение MCP (Model Context Protocol)

## Описание задания

Установить MCP SDK/клиент и написать минимальный код, который создаёт MCP-соединение и получает от него список доступных инструментов.

## Что такое MCP?

**MCP (Model Context Protocol)** — это открытый протокол от Anthropic для стандартизированного подключения различных инструментов и источников данных к LLM-приложениям.

### Преимущества MCP:
- **Стандартизация**: Единый протокол для подключения инструментов
- **Расширяемость**: Легко добавлять новые инструменты
- **Безопасность**: Контролируемый доступ к ресурсам
- **Переиспользование**: Один сервер MCP можно использовать в разных приложениях

## Реализация

### 1. Установка MCP SDK

```bash
pip install "mcp[cli]"
```

### 2. Структура проекта

```
AgentSmith/
├── mcp_server.py      # MCP сервер с набором инструментов
├── mcp_client.py      # MCP клиент для подключения к серверу
└── requirements.txt   # Зависимости проекта
```

### 3. MCP Сервер

**Файл: `mcp_server.py`**

Сервер предоставляет 5 инструментов:

1. **calculator** - Математические операции (сложение, вычитание, умножение, деление)
2. **get_current_time** - Получение текущего времени
3. **text_analyzer** - Анализ текста (подсчет слов, символов, строк)
4. **json_formatter** - Форматирование JSON с настраиваемыми отступами
5. **weather_info** - Демо-информация о погоде

Ключевые компоненты сервера:

```python
from mcp.server import Server
from mcp.types import Tool, TextContent
import mcp.server.stdio

# Создание сервера
server = Server("demo-mcp-server")

# Регистрация списка инструментов
@server.list_tools()
async def list_tools() -> list[Tool]:
    return [Tool(...), ...]

# Обработка вызовов инструментов
@server.call_tool()
async def call_tool(name: str, arguments: Any) -> list[TextContent]:
    # Логика выполнения инструмента
    pass

# Запуск сервера через stdio
async with mcp.server.stdio.stdio_server() as (read_stream, write_stream):
    await server.run(read_stream, write_stream, ...)
```

### 4. MCP Клиент

**Файл: `mcp_client.py`**

Клиент подключается к серверу и выполняет следующие действия:

1. Подключение к MCP серверу через stdio
2. Инициализация сессии
3. Получение списка доступных инструментов
4. Вывод информации о каждом инструменте
5. Тестирование нескольких инструментов

Ключевые компоненты клиента:

```python
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

# Параметры подключения
server_params = StdioServerParameters(
    command="python",
    args=["mcp_server.py"]
)

# Подключение к серверу
async with stdio_client(server_params) as (stdio, write):
    session = ClientSession(stdio, write)
    await session.initialize()

    # Получение списка инструментов
    tools_list = await session.list_tools()

    # Вызов инструмента
    result = await session.call_tool(
        name="calculator",
        arguments={"operation": "add", "a": 15, "b": 27}
    )
```

## Запуск и тестирование

### Запуск клиента

```bash
python mcp_client.py
```

### Ожидаемый результат

```
🎯 MCP Client - День 10 Челленджа

📡 Подключение к MCP серверу...
🔧 Инициализация MCP соединения...
🚀 Запуск MCP сервера...

✅ Соединение установлено успешно!

🔍 Запрашиваем список доступных инструментов...

======================================================================
📋 СПИСОК ДОСТУПНЫХ ИНСТРУМЕНТОВ MCP (5 шт.)
======================================================================

[1] calculator
    Описание: Выполняет базовые математические операции
    Схема входных данных:
    {
      "type": "object",
      "properties": {
        "operation": {"type": "string", "enum": ["add", "subtract", "multiply", "divide"]},
        "a": {"type": "number"},
        "b": {"type": "number"}
      },
      "required": ["operation", "a", "b"]
    }

[2] get_current_time
    Описание: Возвращает текущее время
    ...

[3] text_analyzer
    Описание: Анализирует текст и возвращает статистику
    ...

[4] json_formatter
    Описание: Форматирует JSON с указанными отступами
    ...

[5] weather_info
    Описание: Получает информацию о погоде (демо версия)
    ...

======================================================================
✨ Всего найдено инструментов: 5

======================================================================
🧪 ТЕСТИРОВАНИЕ ИНСТРУМЕНТА: calculator
======================================================================

📊 Результат вызова:
   Результат: 15 add 27 = 42

======================================================================
🧪 ТЕСТИРОВАНИЕ ИНСТРУМЕНТА: text_analyzer
======================================================================

📊 Результат вызова:
   Статистика текста: {
     "words": 13,
     "chars": 87,
     "lines": 3
   }

======================================================================
✅ ТЕСТИРОВАНИЕ ЗАВЕРШЕНО
======================================================================

🎉 Успешно получено 5 инструментов от MCP сервера!
```

## Технические детали

### Транспорт: stdio

MCP поддерживает несколько способов транспортировки:
- **stdio** - через стандартные потоки ввода/вывода (используется в нашем примере)
- **HTTP/SSE** - через HTTP с Server-Sent Events
- **WebSocket** - через WebSocket соединение

### Формат сообщений

MCP использует **JSON-RPC 2.0** для обмена сообщениями между клиентом и сервером.

### Важные моменты

1. **Разделение потоков**: Сервер использует `stderr` для логов, а `stdout` для JSON-RPC коммуникации
2. **Асинхронность**: Весь код использует `async/await` для неблокирующего выполнения
3. **Схемы инструментов**: Каждый инструмент имеет JSON Schema для валидации входных данных

## Практическое применение

MCP можно использовать для:

1. **Подключение к API**: создание инструментов для работы с внешними сервисами
2. **Работа с БД**: инструменты для запросов к базам данных
3. **Файловые операции**: чтение/запись файлов, поиск
4. **Вычисления**: математические операции, анализ данных
5. **Интеграции**: подключение к корпоративным системам

## Следующие шаги

1. Создать собственные инструменты для вашего проекта
2. Интегрировать MCP сервер с существующим агентом
3. Добавить authentication и authorization
4. Реализовать кэширование результатов
5. Создать UI для управления инструментами

## Ссылки

- [MCP Documentation](https://modelcontextprotocol.io)
- [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk)
- [MCP Specification](https://spec.modelcontextprotocol.io)

## Результат

✅ Установлен MCP SDK
✅ Создан MCP сервер с 5 инструментами
✅ Создан MCP клиент для подключения
✅ Реализовано получение списка инструментов
✅ Протестированы вызовы инструментов

**Задание выполнено!** 🎉
