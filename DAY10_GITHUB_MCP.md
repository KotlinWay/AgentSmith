# День 10: Интеграция GitHub MCP сервера

## Задача
Установить MCP SDK/клиент и написать код, который создаёт MCP-соединение с GitHub MCP сервером и получает от него список доступных инструментов.

## Реализация

### 1. Создан GitHub MCP Service (`github_mcp_service.py`)

Сервис для работы с GitHub MCP сервером через npx:

```python
class GitHubMCPService:
    def __init__(self, github_token: str):
        self.github_token = github_token
        self.server_params = StdioServerParameters(
            command="npx",
            args=["-y", "@modelcontextprotocol/server-github"],
            env={"GITHUB_PERSONAL_ACCESS_TOKEN": github_token}
        )

    def get_tools(self) -> List[Dict[str, Any]]:
        # Получает список инструментов от GitHub MCP сервера

    def call_tool(self, tool_name: str, arguments: Dict[str, Any]) -> Dict[str, Any]:
        # Вызывает инструмент GitHub MCP сервера
```

### 2. Добавлены API endpoints в Flask (`app.py`)

#### GET `/mcp/github/tools`
Получает список доступных инструментов GitHub MCP.

**Ответ:**
```json
{
  "status": "ok",
  "count": 15,
  "tools": [...],
  "server": "github"
}
```

#### POST `/mcp/github/call_tool`
Вызывает инструмент GitHub MCP.

**Запрос:**
```json
{
  "tool_name": "search_repositories",
  "arguments": {
    "query": "react",
    "per_page": 10
  }
}
```

### 3. Обновлен UI (`templates/index.html`, `static/mcp.js`, `static/style.css`)

- Добавлен селектор для выбора MCP сервера (Local / GitHub)
- Обновлен JavaScript для работы с разными серверами
- Добавлены иконки для GitHub инструментов
- Добавлены стили для селектора

### 4. Инструменты GitHub MCP

GitHub MCP сервер предоставляет следующие инструменты:

1. **create_or_update_file** 📝 - Создание или обновление файла в репозитории
2. **push_files** ⬆️ - Push файлов в репозиторий
3. **create_repository** 📦 - Создание нового репозитория
4. **get_file_contents** 📄 - Получение содержимого файла
5. **create_issue** 🐛 - Создание issue
6. **create_pull_request** 🔀 - Создание pull request
7. **fork_repository** 🍴 - Форк репозитория
8. **create_branch** 🌿 - Создание новой ветки
9. **list_commits** 📜 - Список коммитов
10. **search_repositories** 🔍 - Поиск репозиториев
11. **search_code** 🔎 - Поиск кода
12. **search_issues** 🔍 - Поиск issues
13. **search_users** 👤 - Поиск пользователей
14. **get_issue** 📋 - Получение информации об issue
15. **update_issue** ✏️ - Обновление issue
16. **add_issue_comment** 💬 - Добавление комментария к issue

## Использование

### Запуск приложения

```bash
python app.py
```

### Использование в UI

1. Откройте http://localhost:5005
2. Перейдите в режим "🔧 MCP Tools"
3. Выберите "🐙 GitHub MCP сервер" в селекторе
4. Нажмите "🔄 Загрузить инструменты"
5. Выберите инструмент из списка
6. Заполните параметры и нажмите "🚀 Выполнить"

### API токен

GitHub Personal Access Token необходимо установить через переменную окружения:

```bash
export GITHUB_TOKEN=your_github_token_here
```

Или добавить в `config.json`:
```json
{
  "github_token": "your_github_token_here",
  ...
}
```

Для создания токена:
1. Перейдите в GitHub Settings → Developer settings → Personal access tokens
2. Создайте новый токен с необходимыми правами (repo, read:user и т.д.)
3. Скопируйте токен и установите его как переменную окружения

## Технические детали

### Транспорт
- **stdio** - через стандартные потоки ввода/вывода
- Сервер запускается через `npx @modelcontextprotocol/server-github`

### Асинхронность
- Весь код использует `async/await` для неблокирующего выполнения
- Синхронные обертки для Flask endpoints

### Безопасность
- GitHub токен передается через переменную окружения
- Результаты вызовов сохраняются в БД памяти

## Структура файлов

```
AgentSmith/
├── github_mcp_service.py      # Сервис для работы с GitHub MCP
├── app.py                     # Flask приложение (обновлено)
├── templates/
│   └── index.html            # UI (обновлено)
├── static/
│   ├── mcp.js               # JavaScript для MCP (обновлено)
│   └── style.css            # Стили (обновлено)
├── test_github_mcp.py        # Тестовый скрипт
└── DAY10_GITHUB_MCP.md      # Эта документация
```

## Результат

✅ Создан GitHub MCP Service
✅ Добавлены API endpoints для GitHub MCP
✅ Обновлен UI для отображения и вызова инструментов
✅ Добавлена поддержка переключения между локальным и GitHub MCP серверами
✅ Код готов к работе с GitHub MCP сервером

**Задание выполнено!** 🎉

## Примечания

При первом запуске npx может потребовать загрузки пакета `@modelcontextprotocol/server-github`.
Убедитесь, что есть доступ к npm registry и интернету.

Для тестирования можно использовать локальный MCP сервер (demo), выбрав "🏠 Локальный MCP сервер" в селекторе.
